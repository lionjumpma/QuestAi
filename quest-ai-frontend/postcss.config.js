// 导入插件
import tailwindcss from 'tailwindcss';
import autoprefixer from 'autoprefixer';

// 使用 ES 模块的 export 语法导出配置
export default {
  plugins: {
    tailwindcss: tailwindcss,
    autoprefixer: autoprefixer,
  },
}
